@manifest_fulfilled_value = 1

# Conquest of territory 
faction_manifest_conquest_of_territory = {
	name = conquest_of_territory_goal_name
	description = conquest_of_territory_goal_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = collection:world_uncapitulated_democratic_countries
		completed_amount_collection = collection:world_uncapitulated_democratic_countries
		range = { 
			max = 1
		}
		
		scale = {
			
		}
		
		progress_sections = {
			
		}
	}
}


# Strength in Unity 
faction_manifest_strength_in_unity = {
	name = strength_in_unity_goal_name
	description = strength_in_unity_goal_desc
	is_manifest = yes
	ratio_progress = {
		total_amount = 20
		completed_amount_collection = {
			input = game:scope
			operators = { faction_members }
			name = COLLECTION_FACTION_MEMBERS
		}
		
		scale = {
			
		}
		
		progress_sections = {
			
		}
	}
}

